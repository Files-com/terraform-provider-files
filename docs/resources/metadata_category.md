---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "files_metadata_category Resource - files"
subcategory: ""
description: |-
  A MetadataCategory defines a reusable set of Custom Metadata rules that can be assigned to folders
  via a folder behavior. Each category specifies named metadata keys with optional allowed-value
  constraints, and a set of default columns to display in the UI.
  If a key's allowed_values array is empty, it is treated as a free-form text field.
  If the array is non-empty, the key is constrained to those values in the Web UI.
---

# files_metadata_category (Resource)

A MetadataCategory defines a reusable set of Custom Metadata rules that can be assigned to folders

via a folder behavior. Each category specifies named metadata keys with optional allowed-value

constraints, and a set of default columns to display in the UI.



If a key's `allowed_values` array is empty, it is treated as a free-form text field.

If the array is non-empty, the key is constrained to those values in the Web UI.

## Example Usage

```terraform
resource "files_metadata_category" "example_metadata_category" {
  name            = "Approval Workflow"
  default_columns = ["Approval Status"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the metadata category.

### Optional

- `default_columns` (List of String) Metadata keys that should appear as columns in the UI by default.

### Read-Only

- `definitions` (Map of List of String) Map of key names to arrays of allowed values. An empty array means free-form text.
- `id` (Number) Metadata Category ID

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Metadata Categories can be imported by specifying the id.
terraform import files_metadata_category.example_metadata_category 1
```
